[package]
name = "sage-dice"
version = "0.1.0"
edition = "2021"
description = "NLP Dice Roller — E2E test target for sage-lore"

[[bin]]
name = "sage-dice"
path = "src/main.rs"

[dependencies]
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
